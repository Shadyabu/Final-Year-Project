1. Install Node.js from https://nodejs.org/en

2. Install npm modules to project directory in terminal;
npm install

4. Start a local server at project directory in terminal:
npx vite

5. You should now see a link similar to http://localhost:5173 appear in terminal. 
Copy and paste it into a browser to view the web application.